# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS
.DS_Store

# Local Claude settings
.claude/settings.local.json

# ---------------------------------------------------------------------------
# LOCAL-ONLY material — never published. These stay on disk but are untracked.
# ---------------------------------------------------------------------------
# Personal notes, planning documents, provenance records, QC one-offs
private/
# The training matrix + manifest (available on request / with the preprint)
training_data/
# Release records: evaluation output, diagnostics, reference joblib
model_somnus_1.0/
# End-user drop point for recordings
UserData/
# Training run outputs (regenerable)
results_generalized_*/
